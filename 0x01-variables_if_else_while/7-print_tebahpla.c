#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char h;

	h = 'z';
	while
		(h >= 'a') {
			putchar(h);
			h++;
		}
	putchar('\n');
	return (0);
}
